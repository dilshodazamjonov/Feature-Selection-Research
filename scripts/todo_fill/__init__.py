"""Fill the ``todo/`` CSV skeletons from frozen evidence plus targeted recomputation.

Entry point: ``scripts/fill_todo.py``.  Every module here is import-light so the
CLI can print help and copy files without loading pandas or the datasets.
"""
