# Prompt 16 outcome-blind execution and resource-pilot plan

Status: authorized for the gated matrix build and fold-1 resource pilot only after this plan is committed.

The canonical protocol lock remains the sole scientific authority. The plan binds its exact raw inventory, 13-method/15-variant order, 27 selector fits, 30 evaluation cells, split/fold identities, seeds, natural-support rules, and resource limits. Neither Prompt 14 numeric results nor any third-dataset outcome has been opened or used.

The real input root is `D:\python projects\Research\data\homecredit_model_stability_2024`. The Prompt-15 adapter must authenticate all 19 included inventory records (18 Parquet files, 962,954,938 bytes) before using a raw file. Depth-2, competition test, submission, duplicates, and unknown files are excluded. The matrix target is 1,526,659 rows, 434 approved raw predictors, and 1,959 deterministic predictors, partitioned into 1,221,743 DEV and 304,916 OOT rows with the exact five temporal folds.

Execution is intentionally sequential: one adapter family, one experiment cell, one fold, zero data-loader workers, and at most four estimator threads. A spawned child and all descendants are sampled every second. The hard controls are a 24 GiB process-tree RSS cap, an 8 GiB system-available-memory abort floor, an 80 GiB results-volume floor, a 20 GiB temporary-volume floor, and a launch-time disk requirement of 2.5 times the remaining 70 GiB output forecast. The stricter 8 GiB memory floor and 2.5x disk factor come from the existing approved execution policy.

The adapter scans base, static, static_cb, and the ten locked depth-1 families in canonical order. It writes one family checkpoint at a time and releases memory before advancing. Atomic manifests authenticate all outputs and `_SUCCESS` is written last. Completed phase/cell checkpoints are immutable and reusable only under the exact protocol, matrix, scope, and selection identities. Incomplete pilot or DEV attempts are archived before an exact retry. Incomplete OOT target-linked evaluation output fails closed.

The pilot is fold 1 only. It executes all 27 registered selector fits and accounts for all 30 model evaluations. Validation and OOT information cannot enter preprocessing, selection, threshold fitting, or model fitting. Natural support is never padded. A failed or infeasible registered cell remains visible.

The pilot is a resource and implementation gate, not a tuning exercise. It must authenticate inventory, matrix dimensions/order/lineage, row uniqueness, target orientation, split/fold membership, prediction alignment, checkpoint identities, stage resources, and complete accounting. A selector or model stopped by its own locked RSS or wall-time cap is sealed as resource-infeasible and remains visible before an exact pilot/DEV resume; OOT never receives this retry path. Measured adapter, selector, model, prediction, metric, and packaging durations will produce the DEV/OOT forecast with a 20% operational allowance. Scientific configuration is unchanged regardless of performance.

The machine-readable plan contains the exact roots, commands, compact cell registry, wall-time limits, forecast, abort mechanics, resume rules, and pass/block criteria.
