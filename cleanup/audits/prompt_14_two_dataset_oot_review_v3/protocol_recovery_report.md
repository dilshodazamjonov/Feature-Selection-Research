# Prompt 14 canonical protocol recovery

## Recovery verdict

The canonical protocol is a deterministic recovery of the unchanged plan committed at `bf2e53e3ea3fbe91e1336019f33b8425287d4564` before numeric outcome inspection. The first blocked attempt later opened numeric artifacts but analyzed zero comparisons, produced no inferential results, did not integrate a report, and never modified the plan. This v3 work does not resume that attempt.

## Binding comparison graph

- Registered comparisons: 124.
- Holm families: 36.
- Baseline versus full/random comparisons: 56.
- Combination comparisons: 68.
- Direction: method minus registered reference.
- Primary inferential metric: locked-OOT ROC AUC.
- Raw test: two-sided paired DeLong on identical authenticated rows.
- Interval: paired target-stratified bootstrap, 2,000 attempts, at least 1,900 valid draws, seed 20260721.
- Multiplicity: Holm step-down within each complete registered family.

The rejected v2 graph is diagnostic evidence only. Its comparison identities, pool100/pool300 voting sensitivities, full/random substitution, 1e-12 tolerance, and incomplete-family rule were not imported. Four primary pool-200 voting relationships remain because the historical plan explicitly registered them.

## Conservative unavailable handling

Every member stays in its original family. A protocol-allowed unavailable or infeasible member exposes no raw p-value, contributes internal Holm input p=1.0, cannot reject, and cannot support superiority. An authentication failure blocks the analysis and can never be converted to p=1.0.

## Natural support

The Home Credit CatBoost cases for `boruta_then_mrmr_mutual_information` and `boruta_then_rfe_catboost` remain requested K=40, realized K=26, `infeasible_natural_support`, and unpadded. They are never described as ordinary budget-matched K=40 results. Only an explicit authenticated realized-K comparator may be described as natural-support matched.

## Safety boundary

No numeric DEV/OOT outcome was opened while recovering and validating this lock. No raw research-data path was resolved, and no worker was started. Persisted outcomes may be authenticated and statistically analyzed only after the lock commit succeeds.
