# LLM cost and scalability inputs

Retained inputs include `artifacts/llm_cache/`, per-run `llm_responses/`,
`llm_call_summary.csv`, `full_llm_cache_summary.csv`, token/call metadata, and
runtime summaries across Home Credit and LendingClub v2 results.

External provider price schedules may be added as dated analysis assumptions;
raw rankings and authenticated cached responses must remain immutable.
