# Current cleanup audit

The complete cleanup transaction inventory, dependency graph, deletion
manifest, validation output, and size report are stored in `cleanup/audit/`.
This directory provides the canonical entry-point link without duplicating the
large inventory files.
